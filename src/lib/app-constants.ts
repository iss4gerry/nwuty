/** Single app calendar boundary (WIB) and default DOB when birth date is not collected in UI. */
export const APP_TIMEZONE = "Asia/Jakarta" as const;
export const DEFAULT_BIRTH_DATE_ISO = "1990-01-01";
